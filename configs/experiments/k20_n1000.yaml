# K=20: Each B maps to 20 different A's, z selects which one
# Higher disambiguation requirement than K=10

defaults:
  - ../base

experiment:
  name: "k20_n1000"

data:
  n_unique_b: 1000
  k: 20
  # n_pairs_effective = n_unique_b = 1000 (same as K=1)
  # Total examples = n_unique_b * k = 20000
