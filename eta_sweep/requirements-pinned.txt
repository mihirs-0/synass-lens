# Pinned environment for the η-sweep NeurIPS submission.
# Versions captured from the local MPS environment on 2026-04-23.
# The Dockerfile installs torch separately (CUDA wheel); everything else
# is pinned here so Docker builds are bit-reproducible.
#
# Non-torch core
numpy==1.26.4
scipy==1.13.1
pandas==2.2.2
matplotlib==3.9.2
pyarrow==16.1.0
tqdm==4.66.5

# Model + config machinery
transformer-lens==2.17.0
einops==0.8.2
omegaconf==2.3.0
hydra-core==1.3.2

# Serialization (transformer-lens depends on this; pin to avoid surprises)
safetensors==0.4.5

# Used by the analysis scripts for logistic / bootstrap fits
statsmodels==0.14.4
